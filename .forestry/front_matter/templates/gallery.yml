---
label: Gallery
hide_body: true
fields:
- name: gallery
  type: field_group_list
  config:
    min: 
    max: 
    labelField: 
  label: Gallery
- name: image
  type: file
  config:
    maxSize: 250
  label: Immagine
